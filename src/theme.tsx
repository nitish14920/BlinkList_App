import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    primary: {
      main: "#2CE080",
    },
  },
});
export const theme2 = createTheme({
  palette: {
    primary: {
      main: "#0365F2",
    },
  },
});
